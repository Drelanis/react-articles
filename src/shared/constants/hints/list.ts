export enum ListView {
  LIST = 'list',
  TILE = 'tile',
}

export enum ListSortField {
  VIEWS = 'views',
  TITLE = 'title',
  CREATED = 'createdAt',
}

export enum ListOrderField {
  DESK = 'desk',
  ASK = 'ask',
}
